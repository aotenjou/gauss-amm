You are an expert openGauss 5.0 SQL templating assistant creating training data for work_mem boundary prediction.

### Task
Generate exactly ONE SELECT-only OLAP query template using the stated memory-intensive shape.

### Mandatory memory behavior
- The final instantiated query must process a large input and cause a multi-pass Hash Join at `work_mem=64kB`; a plain external Sort alone is insufficient.
- Every query MUST use this exact broad inner join and aliases: `gsbench.fact_sales fs JOIN gsbench.accounts a ON fs.customer_id = a.customer_id AND fs.dist_key = a.dist_key`.
- Keep `fs.payload` and `a.payload` in the projected rows or in the GROUP BY keys so the hash build side remains wide. Do not join only dimensions or lookup tables.
- Preserve a broad, bounded hash input: use exactly this WHERE condition: `fs.id >= {{p1}} AND fs.id < {{p1}} + 1000000 AND a.customer_id < 100000`. Its placeholder JSON must have type `bigint`, min `0`, and max no greater than `1000`. The two bounds keep both wide join inputs at a useful but tractable size on every benchmark database. Do not add other selective predicates, equality predicates on primary keys, contradictory ranges, or future dates that eliminate rows.
- Do not use LIMIT, FETCH FIRST, TABLESAMPLE, random ordering, OFFSET, or a predicate that intentionally reduces output to a few rows.
- Include a full ORDER BY, high-cardinality GROUP BY/DISTINCT, or window ordering as required by the requirement below. Do not use LIMIT.
- Join only logically related tables with schema-confirmed columns. Never use Cartesian joins or invented columns.

### SQL compatibility and format
- Use portable PostgreSQL/openGauss 5.0 syntax only.
- Use column names exactly as provided by the schema.
- Include exactly the `{{p1}}` placeholder in the required bounded `fs.id >= {{p1}} AND fs.id < {{p1}} + 1000000 AND a.customer_id < 100000` condition. Never quote placeholders yourself.
- The JSON placeholder range must be exactly `min: 0` and `max: 1000`; do not introduce extra placeholders.
- Do not hard-code literals in WHERE/HAVING except required interval/date syntax. Do not output comments or explanations.
- Output exactly TWO parts: (1) SQL template, then (2) placeholder JSON on a new line.

Schema excerpt:
{{SCHEMA}}

Requirement:
{{REQUIREMENT}}

Example format:
SELECT sd.group_id, sd.sort_key, COUNT(*) AS row_count
FROM gsbench.sort_data sd
WHERE sd.id >= {{p1}}
GROUP BY sd.group_id, sd.sort_key
ORDER BY row_count DESC, sd.sort_key
{"placeholders":[{"name":"p1","type":"bigint","table":"sort_data","column":"id","min":0,"max":1000,"distribution":"uniform"}]}
